[project]
name = "pipeline-tools"
version = "1.1.0"
description = "Multimodal Ingestion, Prompt Workbench & Export Engine"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pixeltable",
    "gradio",
    "pyyaml",
    "pydantic",
    "pandas",
    "google-genai",
]

