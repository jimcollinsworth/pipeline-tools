[project]
name = "pipeline-tools"
version = "0.1.0"
description = "Multimodal Ingestion, Prompt Workbench & Export Engine"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pixeltable",
    "gradio",
    "pymupdf",
    "pyyaml",
    "pydantic",
    "pandas",
    "openai",
    "anthropic",
    "google-genai",
]

